postgresql:
  host: "localhost"
  port: "5432"
  user: "tmp"
  password: "test90123"
  database: "orders_db"

http_server:
  host: ""
  port: "8080"

kafka:
  brokers: "localhost:9092"
  topic: "orders"
  group_id: "order-service-group"
  dlq_topic: "orders-dlq"

cache:
  default_ttl: 1h
  max_size: 1000

migrations_path: "migrations"

telemetry:
  otlp_endpoint: "localhost:4318"  # для OTLP HTTP (без http://)
  metrics_port: "2112"